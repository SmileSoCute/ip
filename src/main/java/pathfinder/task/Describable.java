package pathfinder.task;

/** Represents an object that has a user-facing description. */
public interface Describable {
    /**
     * Returns the object's description.
     *
     * @return the description shown to the user
     */
    String getDescription();
}
